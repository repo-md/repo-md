// Import only the OpenAI tool specs
import { OpenAiToolSpec, toolSpecs } from "./openai/OpenAiToolSpec.js";

// Export only the tool specs (no handlers or functions)
export {
  OpenAiToolSpec,
  toolSpecs
};
